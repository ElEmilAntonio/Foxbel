export class Track{
    constructor(artist, title, preview, album, albumTitle){
        this.artist = artist,
        this.title = title,
        this.preview = preview,
        this.album = album,
        this.albumTitle = albumTitle
    }
}